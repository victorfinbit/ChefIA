[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
 
[functions.chat]
  timeout = 30
 
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
